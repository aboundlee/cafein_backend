
# run docker app
docker run -p 3000:3000 aboundlee/node-web-app


# if there is error message "Cannot connect to the Docker daemon at unix:///var/run/docker.sock. Is the docker daemon running?"
open --background -a Docker
